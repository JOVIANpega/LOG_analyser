# FAIL 匯總結構分析與改進建議

## 1. 為什麼會產生目前的結構？

經過分析原始程式碼 (`excel_writer.py`)，我發現目前的結構是基於以下設計理念：

- **報告導向 (Report-Oriented)**：程式碼的意圖是生成一份「可閱讀的文件」，而不是「數據表格」。它使用了大量的格式化（字體、顏色、分隔線）來模擬一份紙本報告。
- **完整性優先**：為了不遺漏任何錯誤細節，程式將所有相關的 Log（指令、回應、錯誤訊息）全部打包塞進一個儲存格，並設定自動換行。
- **互動式瀏覽**：程式花了很多力氣建立「目錄」和「跳轉連結」，希望使用者能像瀏覽網頁一樣在 Summary 和詳細 Log 之間切換。

## 2. 優缺點分析

### 🟢 優點
- **資訊完整**：保留了錯誤發生的完整上下文，不會因為欄位限制而截斷資訊。
- **互動方便**：提供了方便的跳轉連結，可以快速查看詳細 Log。
- **視覺提示**：使用了顏色（紅色 FAIL）和註解預覽功能。

### 🔴 缺點
- **資訊過載 (Information Overload)**：所有細節擠在一起，導致「雜訊」淹沒了「訊號」。使用者很難從一大段文字中一眼看到 `Segmentation fault` 這樣的關鍵字。
- **缺乏結構**：Log 檔名和錯誤訊息都是非結構化的長字串，無法篩選（例如：無法只看特定 SN 的錯誤）。
- **空間浪費**：大量的分隔線和導航連結佔用了寶貴的螢幕空間，降低了閱讀效率。
- **難以閱讀**：使用者必須手動調整欄寬或行高，才能看清內容。

## 3. 為什麼「優化版」還是不夠好？

即使將資料拆分成表格，如果**內容本身沒有被提煉**，使用者依然會迷失在大量的文字中。
使用者真正需要的是：**「一句話告訴我錯在哪裡」**，而不是「把所有錯誤 Log 丟給我自己看」。

## 4. 具體改進建議

我建議採取 **「多層次顯示 + 關鍵字提取」** 的策略：

### ✅ 1. 提煉關鍵錯誤 (Key Error Extraction)
不要顯示完整的 Log，而是由程式自動提取關鍵字：
- **原始**: `... executes fail. Reply="0\nSegmentation fault (core dumped)...`
- **提煉後**: `Segmentation Fault` 或 `Execution Failed`

### ✅ 2. 結構化欄位 (Structured Columns)
將長字串拆解為獨立欄位，方便篩選：
- **SN**: `WE253600014`
- **測項**: `4cam stitching1`
- **錯誤類型**: `Segmentation Fault`

### ✅ 3. 分層顯示 (Layered Display)
- **Summary 頁面**: 只顯示最精簡的資訊（SN、測項、錯誤類型）。
- **詳細內容**: 
  - **方法 A**: 透過「超連結」跳轉到獨立 Sheet 查看完整 Log。
  - **方法 B**: 將完整 Log 放在 Excel 的「註解 (Comment)」中，滑鼠移過去才顯示。

### ✅ 4. 視覺降噪
- 移除所有分隔線 (`───`) 和裝飾文字 (`===`)。
- 使用標準的 Excel 表格格式。

---

### 🚀 建議的最終樣式

| 狀態 | SN | 測項 | 錯誤類型 (關鍵字) | 發生時間 | 詳細 Log |
| :---: | :--- | :--- | :--- | :--- | :--- |
| 🔴 FAIL | WE253600014 | 4cam stitching1 | **Segmentation Fault** | 10:43:58 | [查看] |
| 🔴 FAIL | WE253600009 | 4cam stitching1 | **Value Mismatch** | 13:55:20 | [查看] |

這樣的結構可以讓您**一眼看出**：
1. 哪些機器 (SN) 失敗了？
2. 主要的錯誤原因是什麼？
3. 如果需要細節，再點擊 [查看]。
