# Log 解析邏輯優化記錄 (2025-12-26)

## 🔍 發現的問題：PASS/FAIL 誤判

在解析 `MINE\LOG` 目錄下的日誌時，發現部分 FAIL 日誌被錯誤判定為 PASS，主要原因如下：

1.  **狀態覆蓋問題**：
    *   **現象**：某些測項步驟（Step）中間已經印出 `ERROR` 或 `fail` 關鍵字，但因為該步驟結尾印出了 `@STEPxxx@... Test is Pass !`，解析器會以後者為準，將整個步驟標記為 PASS。
    *   **影響**：導致真正的錯誤訊息在統計中消失。

2.  **異常中斷（Aborted/Timeout）未偵測**：
    *   **現象**：Log 檔案在執行到一半時突然結束，沒有印出任何 PASS 或 FAIL 標籤。
    *   **影響**：解析器因為沒看到錯誤關鍵字，預設將未完成的步驟視為 PASS。

3.  **檔名判斷不完全**：
    *   **規則**：在現有的測試流程中，檔名若未包含 `PASS` 字樣，通常代表該 Log 為 FAIL 或是特定測項失敗（如：`...-VSCH026-043.log`）。
    *   **現象**：舊邏輯雖然會因為檔名沒 PASS 而進入 `_parse_fail_log` 流程，但若內容沒噴錯，最終結果仍會顯示為 PASS。

---

## 🛠️ 已實施的修復措施

### 1. 強化步驟狀態鎖定
*   **邏輯變更**：在 `_parse_fail_log` 中，一旦在步驟內偵測到任何 `fail_keywords`（如 FAIL, ERROR, NACK, timeout 等），該步驟的狀態將被鎖定為 **FAIL**。
*   **效果**：後續即使出現 `Test is Pass !` 也無法覆蓋已發生的錯誤。

### 2. 嚴格的中斷偵測機制
*   **邏輯變更**：處理最後一個測項（Last Step）時，若沒有看到明確的 PASS 結束標籤且檔案已結束，則強制判定為 **FAIL**。
*   **錯誤標註**：自動加上 `Step not completed (Log abruptly ended)` 字樣。

### 3. 基於檔名的防禦性判斷
*   **邏輯變更**：如果檔名中完全沒有 `PASS` 字樣，且內容掃描完畢後仍未發現明確錯誤，則會針對最後一個步驟強制標記 FAIL。
*   **錯誤標註**：標註為 `Step incomplete or Fail (Filename does not contain 'PASS')`。

### 4. 關鍵字掃描優化
*   **邏輯變更**：統一將 `fail_keywords` 擴展並在比對時進行大寫轉換（`upper()`），確保不會因為大小寫差異漏掉錯誤（例如：`fail` vs `FAIL`）。

---

## 📝 測試建議
*   **目標檔案**：`1+4cam stitching1 test-20251226095927.log` (已確認會正確標記為中斷 FAIL)。
*   **目標檔案**：`MINE\LOG\` 下所有檔名末尾為測項編號（如 `-VSCH026-043`）而非 `-PASS` 的檔案。

---
---

## 🚀 V2.10.0 邏輯更新 (2025-12-30)

### 1. 比對項目解析規則 (Validation Parsing) 重大優化
*   **嚴格模式實施**：為了排除背景雜訊（如 16 進制轉換、座標、時間戳），解析器現在僅抓取符合 `名稱 = 數值 (判定標準)` 的行。
*   **括號內容過濾**：
    *   **逗號強制要求**：括號內必須包含「逗號」（例如 `(10,50)` 或 `(100,)`），這能精準過濾掉非測試項目的括號（如 `(51)`）。
    *   **日期過濾**：自動跳過括號內包含英文月份（如 `Jan`, `Feb`...）的系統日期行。
*   **名稱身分驗證**：
    *   名稱中必須包含英文字母或底線，排除純數字序列。
    *   黑名單過濾：自動排除 `ISN`, `DATE`, `VERSION`, `OFFSET` 等系統狀態欄位。
*   **分隔符限制**：僅支援 `=` 作為有效比對分隔符，排除內容打印中的冒號 `:`。

### 2. Excel 匯總報表穩定性增強
*   **X 軸標籤強制字串化**：在 PASS 匯總圖表的 ISN 標籤前加上 `#` 符號（如 `#2025...`），解決了部分 Excel 版本將長序號誤認為數值導致座標軸崩潰、圖表消失的問題。
*   **欄位分割排版**：PASS 摘要表正式改為「ISN (連結)」、「檔案分析」、「站別」、「時間 (Sec)」、「狀態」獨立五欄。

---
**記錄人員**：Antigravity (AI Assistant)  
**日期**：2025-12-30

