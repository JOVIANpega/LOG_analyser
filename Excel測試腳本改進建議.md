# Excel 測試腳本改進建議報告

## 📋 分析檔案
- **檔案名稱**: `VALO360_TestFlow_AOCI_4Cam_Stitching_test1_FATP_250924B.xlsx`
- **測試工作表**: `4cam stitching1 test`
- **總行數**: 1,497 行 (含標題)
- **總列數**: 21 列

---

## 📊 當前狀態統計

### Ref 欄位使用情況
- **總測試步驟**: 1,496 行
- **有使用 Ref 的行數**: 21 行
- **使用率**: **1.4%** ⚠️
- **未使用 Ref 的行數**: 1,475 行 (98.6%)

### Validation 欄位使用情況
- **總測試步驟**: 1,496 行
- **有使用 Validation 的行數**: 21 行
- **使用率**: **1.4%** ⚠️
- **未使用 Validation 的行數**: 1,475 行 (98.6%)

### 交叉分析
- **同時有 Ref 和 Validation**: 20 行 (1.3%) ✓
- **只有 Ref 沒有 Validation**: 1 行 (0.1%) ⚠️
- **只有 Validation 沒有 Ref**: 1 行 (0.1%)
- **兩者都沒有**: 1,474 行 (98.5%)

---

## 🔍 發現的問題

### 1. ⚠️ **Ref 和 Validation 欄位使用率都極低 (各 1.4%)**

**問題描述**:
- 在 1,496 個測試步驟中,只有 21 行有使用 Ref 提取變數
- 同樣只有 21 行有設定 Validation
- 這意味著 **98.6% 的測試步驟既沒有提取變數,也沒有進行數據驗證**
- 大部分測試步驟可能只是簡單的 Send/Reply,缺少深度的數據分析

**影響**:
- 測試自動化程度極低
- 無法追蹤和驗證關鍵測試數據
- 難以進行數據分析和趨勢追蹤
- 測試結果主要依賴人工判讀

**好消息**:
在有使用 Ref 的 21 行中,有 20 行 (95.2%) 都有對應的 Validation,這顯示:
- 工程師已經理解 Ref 和 Validation 應該配對使用
- 現有的 Ref/Validation 配置質量很高
- 只需要擴大使用範圍即可

**範例**:
```
Row 14: 
✓ Ref: $ISN:WithinString,"dev sn","\n":Trim:rpString," ",""
✓ Validation: (ISN,%len,11,11);

Row 36-39: 溫度監控
✓ Ref: temp0:maString,"s0\r\n":mbString,"\r\nroot";
✓ Validation: (temp0,%d,-999,999);
```

**唯一的例外**:
```
Row 22: 
✓ Ref: $time
✗ Validation: (無) ← 建議添加時間格式驗證
```

---

### 2. 📝 **Validation 格式不夠智能**

**當前格式**:
```
(變數名,%格式,下限,上限);
```

**問題**:
- 使用固定的上下限 (如 -999, 999)
- 無法根據實際提取的數值動態調整
- 缺少 ±10% 的智能驗證

**改進建議**:
根據之前的對話歷史,應該實現:
```
(變數名,%格式,±10%);  // 自動計算 ±10% 範圍
```

或者:
```
(變數名,%格式,提取值*0.9,提取值*1.1);  // 明確的 ±10% 範圍
```

---

### 3. 🔄 **Ref 公式可以優化**

**當前常見格式**:
```
$變數名:maString,"關鍵字=":mbString,"%結束標記";
```

**問題**:
- 部分公式可能過於複雜
- 缺少 `:Trim` 處理空白字符
- 某些場景可以使用更簡潔的 `WithinString`

**改進建議**:
```
// 建議加入 :Trim 處理
$變數名:maString,"關鍵字=":mbString,"%結束標記":Trim;

// 對於 key=value 格式,可使用 WithinString
$變數名:WithinString,"關鍵字=","%結束標記":Trim;
```

---

### 4. 📋 **多變數提取缺少分組驗證**

**當前情況**:
某些行提取了多個相關變數,但驗證分散或缺失:

```
Row 325: 提取 15 個 dre_off_cam01 相關參數
Ref: $dre_off_cam01_MAX_PixelsShift_0:...; $dre_off_cam01_MAX_PixelsShift_1:...; ...
Validation: (dre_off_cam01_MAX_PixelsShift_0,%f,-999,999); ... (15 個驗證)
```

**改進建議**:
- 對於批量提取的變數,應該確保每個都有對應的驗證
- 可以考慮使用範圍驗證或相對驗證

---

### 5. 🎯 **缺少錯誤處理機制**

**問題**:
- 沒有看到針對提取失敗的處理
- 缺少預設值設定
- 沒有異常情況的備用方案

**建議**:
- 為關鍵變數設定預設值
- 加入提取失敗的錯誤處理
- 對於可選變數,提供容錯機制

---

## 💡 具體改進建議

### 優先級 1: 🔴 **大幅增加 Ref 和 Validation 的使用**

**目標**: 將 Ref 和 Validation 使用率從 1.4% 提升到至少 30%

**現狀分析**:
- 目前只有 21/1,496 (1.4%) 的測試步驟有使用 Ref/Validation
- 這表示大部分測試步驟只是簡單的命令執行,沒有數據提取和驗證
- 需要識別哪些測試步驟應該添加 Ref/Validation

**行動方案**:

1. **識別需要數據提取的測試步驟** (第一優先)
   - 查看 Reply 欄位中包含關鍵數據的行
   - 尋找包含數值、序號、狀態等資訊的回應
   - 優先處理:
     - 序號/ID (如 SN, Device ID)
     - 溫度、電壓等感測器數據
     - 測試結果數值 (如 PixelsShift, discontinue)
     - 狀態碼、錯誤碼

2. **為識別出的步驟添加 Ref 公式**
   - 使用 `WithinString` 提取 key=value 格式
   - 使用 `maString`/`mbString` 提取特定範圍
   - 加入 `:Trim` 處理空白字符

3. **為每個 Ref 添加對應的 Validation**
   - 數值型: 設定合理的上下限
   - 字串型: 驗證長度或格式
   - 確保 Ref 和 Validation 配對

**範例**:

**情境 1: 提取序號**
```
// 原本
Send: cat /sys/class/dmi/id/product_serial
Reply: (回應內容)

// 改進後
Send: cat /sys/class/dmi/id/product_serial
Reply: (回應內容)
Ref: $ProductSN:WithinString,"serial","\n":Trim
Validation: (ProductSN,%len,10,20);  // 假設序號長度 10-20
```

**情境 2: 提取測試數據**
```
// 原本
Send: run_test
Reply: MAX_PixelsShift_0=1.5pixels...

// 改進後
Send: run_test
Reply: MAX_PixelsShift_0=1.5pixels...
Ref: $PixelShift:maString,"MAX_PixelsShift_0=":mbString,"pixels":Trim
Validation: (PixelShift,%f,0,10);  // 假設正常範圍 0-10
```

**預期成果**:
- 識別出至少 400-500 個需要數據提取的測試步驟
- 將 Ref/Validation 使用率提升到 30% 以上
- 建立完整的測試數據追蹤系統

---

### 優先級 2: 🟡 **實現智能 ±10% 驗證**

**目標**: 根據提取的實際數值自動計算驗證範圍

**實現方式**:
1. 在程式中解析 Ref 提取的數值
2. 自動計算 ±10% 範圍
3. 生成動態 Validation 語句

**範例**:
```python
# 假設提取到的值是 100
extracted_value = 100
lower_bound = extracted_value * 0.9  # 90
upper_bound = extracted_value * 1.1  # 110

validation = f"({var_name},%f,{lower_bound},{upper_bound});"
```

---

### 優先級 3: 🟢 **優化 Ref 公式**

**目標**: 提高提取的準確性和穩定性

**改進點**:
1. **加入 `:Trim`**: 移除前後空白
   ```
   $var:maString,"key=":mbString,"end":Trim;
   ```

2. **使用 `WithinString`**: 簡化 key=value 提取
   ```
   $var:WithinString,"key=","end":Trim;
   ```

3. **避免模糊分隔符**: 不使用 `\nD` 等短分隔符

---

### 優先級 4: 🟢 **加強錯誤處理**

**建議**:
1. 為關鍵變數設定預設值
2. 加入提取失敗的日誌記錄
3. 對於可選參數,使用容錯機制

---

## 📈 改進後預期效果

### 測試覆蓋率提升
- **Ref 使用率**: 1.4% (21行) → 30%+ (450行)
- **Validation 使用率**: 1.4% (21行) → 30%+ (450行)
- **數據追蹤能力**: 提升 20 倍以上

### 測試質量提升
- ✅ 建立完整的測試數據追蹤系統
- ✅ 自動檢測異常數值
- ✅ 減少人工檢查工作量
- ✅ 提高測試可靠性
- ✅ 支援數據分析和趨勢追蹤

### 維護性提升
- ✅ 公式更簡潔易讀 (使用 WithinString 和 :Trim)
- ✅ Ref 和 Validation 配對完整
- ✅ 錯誤處理更完善
- ✅ 自動化程度更高

### 實際價值
- 📊 可以追蹤每次測試的關鍵數據
- 📈 可以進行歷史數據分析
- 🔍 更容易發現異常趨勢
- ⚡ 減少人工判讀時間

---

## 🛠️ 實施步驟

### 第一階段: 分析與規劃 (1-2 天)
1. 識別所有需要 Validation 的 Ref 變數
2. 分類變數類型 (數值/字串/布林)
3. 確定驗證規則

### 第二階段: 批量添加 Validation (3-5 天)
1. 為數值型變數添加範圍驗證
2. 為字串型變數添加格式驗證
3. 測試驗證規則的有效性

### 第三階段: 優化與測試 (2-3 天)
1. 優化 Ref 公式
2. 實現 ±10% 動態驗證
3. 完整測試驗證

---

## 📝 總結

當前測試腳本的主要問題是 **Ref 和 Validation 使用率都極低**,各只有 1.4% 的測試步驟有使用。這導致:
- 98.6% 的測試步驟沒有數據提取和驗證
- 無法追蹤關鍵測試數據
- 測試自動化程度極低
- 難以進行數據分析和趨勢追蹤

**好消息**:
- 現有的 21 個 Ref/Validation 配置質量很高 (95.2% 配對率)
- 工程師已經掌握正確的使用方式
- 只需要擴大使用範圍即可

**核心改進方向**:
1. 🔴 **大幅增加 Ref 和 Validation 使用** (從 1.4% 到 30%+)
   - 識別需要數據提取的測試步驟 (400-500 個)
   - 為關鍵數據添加 Ref 公式
   - 為每個 Ref 配對 Validation
2. 🟡 **實現智能 ±10% 動態驗證**
   - 自動計算合理的驗證範圍
   - 提高驗證的準確性
3. 🟢 **優化 Ref 公式**
   - 使用 WithinString 簡化提取
   - 加入 :Trim 處理空白
   - 提高公式的可讀性
4. 🟢 **加強錯誤處理機制**
   - 為關鍵變數設定預設值
   - 提供容錯機制

透過這些改進,可以將測試腳本從「簡單的命令執行」提升為「完整的數據追蹤和驗證系統」,大幅提升測試的價值和自動化程度。

---

**報告生成時間**: 2025-12-08  
**分析工具**: Python + openpyxl  
**分析者**: AI Assistant
