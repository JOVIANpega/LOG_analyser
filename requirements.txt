# PEGA Log Analyzer 依賴套件清單

# 核心依賴
tkinter  # Python內建，但某些Linux發行版需要額外安裝

# Excel處理
openpyxl>=3.0.0

# 資料處理
pandas>=1.3.0

# 壓縮檔案支援
py7zr>=0.20.0  # 7Z檔案支援
rarfile>=4.0    # RAR檔案支援

# 其他工具
pathlib2>=2.3.0  # 路徑處理（Python 3.4+內建）

# 可選依賴（提升功能）
pillow>=8.0.0    # 圖片處理（如果需要圖標支援）
numpy>=1.20.0    # 數值計算（pandas依賴）

# 開發依賴（可選）
pytest>=6.0.0   # 測試框架
black>=21.0.0   # 程式碼格式化
flake8>=3.8.0   # 程式碼檢查